/* ========================
   FILTER SIDEBAR
======================== */

.filter-sidebar {
  min-width: 220px;
  max-width: 240px;
  flex-shrink: 0;
}

.sidebar-title {
  font-size: 1.3rem;
  font-weight: 700;
  color: #1a1a1a;
  margin-bottom: 0.3rem;
}

.sidebar-subtitle {
  font-size: 0.95rem;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 0.6rem;
}

/* ── Dropdown Group ── */
.filter-dropdown-group {
  margin-bottom: 0.5rem;
}

.filter-label {
  font-size: 0.88rem;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 0.3rem;
  margin-top: 0.2rem;
}

.bold-label {
  font-size: 1rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  margin-top: 0;
}

/* ── Bags dropdown button ── */
.filter-dropdown-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  padding: 0.55rem 0.9rem;
  font-size: 0.92rem;
  color: #1a1a1a;
  background: #fff;
  cursor: pointer;
  transition: border-color 0.2s;
}

.filter-dropdown-btn:hover,
.filter-dropdown-btn.open {
  border-color: #8b5cf6;
}

.dropdown-chevron {
  font-size: 0.75rem;
  color: #555;
}

/* ── Flat category buttons (Wallet, Belt) ── */
.category-flat-btn {
  width: 100%;
  display: flex;
  align-items: center;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  padding: 0.55rem 0.9rem;
  font-size: 0.92rem;
  color: #1a1a1a;
  background: #fff;
  cursor: pointer;
  margin-bottom: 0.5rem;
  text-align: left;
  transition: border-color 0.2s, background 0.2s, color 0.2s;
}

.category-flat-btn:hover {
  border-color: #8b5cf6;
  background: #f5f3ff;
}

.category-flat-btn.active {
  border-color: #8b5cf6;
  background: #f5f3ff;
  color: #7c3aed;
  font-weight: 600;
}

/* ── Dropdown List ── */
.filter-dropdown-list {
  border: 1px solid #d1d5db;
  border-top: none;
  border-radius: 0 0 8px 8px;
  background: #fff;
  padding: 0.4rem 0;
}

.filter-checkbox-item {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  padding: 0.45rem 1rem;
  font-size: 0.9rem;
  color: #1a1a1a;
  cursor: pointer;
  transition: background 0.15s;
}

.filter-checkbox-item:hover {
  background: #f5f3ff;
}

.filter-checkbox-item.selected {
  background-color: #f5f3ff;
}

.filter-checkbox-item input[type='checkbox'] {
  accent-color: #8b5cf6;
  width: 15px;
  height: 15px;
  cursor: pointer;
  flex-shrink: 0;
}

/* ── Brands section ── */
.filter-section {
  margin-bottom: 0.8rem;
}

.brand-list {
  display: flex;
  flex-direction: column;
  gap: 0.3rem;
}

.brand-item {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  padding: 0.4rem 0.5rem;
  font-size: 0.92rem;
  color: #1a1a1a;
  cursor: pointer;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  background: #fff;
  transition: background 0.15s, border-color 0.15s;
}

.brand-item:hover {
  background: #f5f3ff;
  border-color: #8b5cf6;
}

.brand-item.selected {
  background: #f5f3ff;
  border-color: #8b5cf6;
}

.brand-checkbox-box input[type='checkbox'] {
  accent-color: #8b5cf6;
  width: 16px;
  height: 16px;
  cursor: pointer;
}

/* ── Price Radio Group (NEW - replaces range slider) ── */
.filter-price-section {
  margin-top: 0.8rem;
  margin-bottom: 0.8rem;
}

.price-radio-group {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-top: 0.5rem;
}

.price-radio-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
  font-size: 0.9rem;
  color: #4b5563;
  transition: color 0.2s;
}

.price-radio-item:hover {
  color: #7c3aed;
}

.price-radio-item input[type="radio"] {
  width: 16px;
  height: 16px;
  cursor: pointer;
  accent-color: #8b5cf6;
  margin: 0;
}

.price-radio-item span {
  cursor: pointer;
}

.clear-price-btn {
  margin-top: 0.5rem;
  padding: 0.35rem 0.75rem;
  background: #fff;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  color: #6b7280;
  font-size: 0.8rem;
  cursor: pointer;
  transition: all 0.2s;
  width: fit-content;
  font-weight: 500;
}

.clear-price-btn:hover {
  background: #fef2f2;
  border-color: #fca5a5;
  color: #dc2626;
}

/* Remove old price range slider styles (optional - keep for compatibility) */
.price-range-slider,
.price-range-inputs {
  display: none;
}

/* ── Apply Button ── */
.apply-filter-btn {
  width: 100%;
  background-color: #8b5cf6;
  color: #fff;
  border: none;
  border-radius: 10px;
  padding: 0.75rem;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  margin-top: 1.2rem;
  transition: background 0.2s;
}

.apply-filter-btn:hover {
  background-color: #7c3aed;
}

/* ── Responsive ── */
@media (max-width: 992px) {
  .filter-sidebar {
    min-width: 100%;
    max-width: 100%;
  }
}








